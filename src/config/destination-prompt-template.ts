export const destinationPromptTemplate = (destination: string) => `
Generate **ONLY** a valid JSON object for the destination ${destination}. That means **NO ARRAYS**. **DO NOT** include a user_ratings field. Temperature will be stored as a **NUMBER**. If you include any additional text outside the JSON it will break. **DO NOT** include the destinationId field in your output. Include **only** the following fields for each destination: destinationName, latitude, longitude, description, country, bestTimeToVisit, averageTemperatureLow, averageTemperatureHigh, popularActivities, travelTips, nearbyAttractions, transportationOptions, accessibilityInfo, officialLanguage, currency, localCuisine, userRatings, costLevel, safetyRating, and culturalSignificance.
`;
